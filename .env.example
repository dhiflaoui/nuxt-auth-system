DATABASE_URL="" # This is the connection string to your database that Prisma will use to connect to your database. You can get this from the Prisma dashboard.
PULSE_API_KEY="" # This is the API key that you will use to authenticate with the Pulse API. You can get this from the Pulse dashboard.
JWT_SECRET="" # This is the secret that will be used to sign the JWT tokens. You can generate a random string using the command `openssl rand -hex 32`